import { FilesizepipePipe } from './filesizepipe.pipe';

describe('FilesizepipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilesizepipePipe();
    expect(pipe).toBeTruthy();
  });
});
